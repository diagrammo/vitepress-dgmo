{
  "name": "vitepress-dgmo-fixture",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "if command -v claim-port >/dev/null 2>&1; then claim-port 4346 || { [ $? -eq 3 ] || exit 1; exit 0; }; fi; vitepress dev --port 4346 --strictPort",
    "docs:build": "vitepress build",
    "docs:preview": "if command -v claim-port >/dev/null 2>&1; then claim-port 4346 || { [ $? -eq 3 ] || exit 1; exit 0; }; fi; vitepress preview --port 4346"
  },
  "dependencies": {
    "@diagrammo/dgmo": "0.60.0",
    "vitepress": "^1.6.4",
    "vitepress-dgmo": "link:../.."
  },
  "devDependencies": {
    "@mdit-vue/shared": "^3.0.2"
  },
  "engines": {
    "node": ">=20.6"
  }
}
